/**
 *  Функция меняющая размер элемента с числом
 */

define('game/Helpers/ResizeNumber.js', function () {

    return {
        resizeNumber: function (number, minNumber, maxNumber, arrayNumberUI) {
            function resizeAll() {}
        }
    }
});